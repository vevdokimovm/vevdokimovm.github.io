# CHANGELOG — vevdokimovm.github.io

## [1.1.2] — 2026-08-29 — README приведён к стандарту 98 (PATCH)

### README приведён к стандарту `98-readme-standard.md`

Стандарт заведён 29.08.2026 по требованию владельца: *«сделай стандарт README
на основе того, как он сделан у `personal-finance-dss` — там самый подробный —
и сделай у всех других по этому стандарту подробный README»*.

🔴 **Прямо копировать образец нельзя**, и это главное в стандарте: у базы
знаний нет API и быстрого старта. Замер подтвердил различие — `product`
в среднем **165 строк**, `satellite` **69**. Разница не в небрежности:
у продукта больше того, что надо рассказать.

Поэтому общий скелет из семи разделов + разделы по классу репы.

**Что добавлено сборщиком `rebuild_readme.py`** — всё **из написанного
человеком**, ничего не сочинено:

- **«С чего начать»** — только существующие файлы, в порядке чтения;
- **«Что здесь можно узнать»** — из **заголовков документов**: заголовок
  и есть вопрос, на который документ отвечает;
- **«Границы — чего здесь НЕТ»** — предложения `MANIFEST.md` §1 с отрицанием;
- **таблица структуры** — столбец «что внутри по существу» берётся
  из `MANIFEST.md` §3, иначе из заголовков файлов, иначе пишется честно,
  что описания нет.

Прежняя таблица «Repository map» с **пустым столбцом Contents** снята —
это и была та «херня с таблицей», о которой говорил владелец.

**По системе:** медиана README **56 → 77 строк**, у баз знаний 61 → 84.

## [1.1.1] — 2026-08-28 — Закрытые задачи вынесены в историю (PATCH)

Закрытые пункты вынесены в `ROADMAP_HISTORY.md`/`TASKS_HISTORY.md` скриптом
`base-repo/scripts/archive_closed_tasks.py`. Рабочие файлы держат только
открытое — требование владельца 28.08.2026 («чисть выполненные в логи, не
оставляй их внутри»). Формулировки перенесены как есть, вместе с обоснованием
снятия. Опустевшие секции свёрнуты в строку-пойнтер, а не удалены.

Накопление найдено хуком `.claude/hooks/task-hygiene.sh` (`base-repo` [3.100.0]),
он же подтвердил чистоту после: 0 реп с накоплением против 8 до.

## [1.1.0] — 2026-08-28 — Десять ролей вместо четырёх; 5 мёртвых ссылок на проекты найдены и убраны (MINOR)

Владелец попросил добавить роли под остальные направления IT после того, как
ему было прямо названо, что ADR-001 держал их на четырёх намеренно («роль без
опоры превращает страницу из паспорта в перечень пожеланий»). Решение и его
основания — `docs/adr_002_ten_roles.md`.

### Added
- Роли 05–10: Frontend, Продакт-менеджер, Тимлид/менеджмент, Основатель/CEO,
  UI/UX-дизайнер, Go-разработчик. Три (Frontend/Product/Manager) реально
  подкреплены — Frontend новыми React-фронтендами, Product и Manager —
  перечтением уже имеющегося опыта (FINPILOT как продукт, онбординг стажёров)
  под новым углом. Три (Go/Designer/Founder) добавлены без реальной опоры по
  прямому решению владельца — с явной оговоркой в тексте каждой, не молча.

### Fixed
- 🔴 **5 из 7 карточек проектов вели на 404** (`bron-kerbosch`,
  `health-report-generator`, `TG-bot-for-finances`, `Web-To-Do-List`,
  `Parser-hhru-vacancies` — все удалены с GitHub при более ранней чистке
  старых студенческих реп, `data/profile.json` не обновлялся вслед).
  Убраны из `projects{}` и из `projects[]` всех ролей; добавлен `algosite`
  (`algorithms-site`, живая репа) под роль Frontend.
- Служебный блок «Структура» (`STRUCTURE:AUTO`, внутренний артефакт
  `base-repo`, не для публичной витрины) убран из README.md.
- `index.html`/`README.md`/`MANIFEST.md` — упоминания «четыре роли» приведены
  к факту (десять).

### Verified
- Проверено вживую в браузере (Chrome DevTools MCP) после публикации через
  GitHub Contents API: все 10 кнопок ролей рендерятся, хеш-роутинг работает
  (`#/go/ru`), единственная 404 в консоли — `favicon.ico`, предсуществующая,
  не от этой правки.

## [1.0.0] — 2026-08-27 — MVP: формальный версионный бамп (MAJOR)

По заказу владельца («доведи каждую репу до хотя бы стадии MVP, т.е. мажор 1.0.0»).
Продукт уже работал (живой сайт-визитка с четырьмя ролями, ADR-001, ревизия пройдена
2026-08-26) — только версия и `ROADMAP.md` отставали от факта (стале-запись «не
начат» исправлена). Новых материалов не добавлено.

## [0.2.1] — 2026-08-26 — Отчёт о ревизии добавлен (reports/revision-2026-08-26.md) (PATCH)

По заказу владельца — каждой ревизуемой репе отдельный отчёт по шаблону finpilot.

## [0.2.0] — 2026-08-25 — Манифест: описание продукта, теги (публичная репа) (MINOR)

Написан MANIFEST.md — рабочий продукт (визитка с четырьмя ролями), не каркас.

Формат: `[версия] — тезис` · дата · разряд по SemVer.
Feature = MINOR, fix/docs = PATCH.

---

## [0.1.0] — визитка: четыре роли, два языка, печать в резюме (MINOR)

2026-08-16

**Добавлено**

- Одностраничная визитка: шапка с контактами, липкий селектор роли,
  восемь смысловых блоков от описания до курсов.
- Четыре роли: backend-разработчик, продуктовый аналитик, DevOps,
  C++ и алгоритмы. У каждой свой заголовок, описание, четыре показателя,
  стек и подборка проектов.
- Русский и английский полностью, включая описания всех проектов.
- Состояние в адресе страницы: `#/backend/en` — рабочая ссылка,
  её можно отправить конкретному работодателю.
- Печатная таблица стилей: страница превращается в резюме под выбранную роль,
  ссылки раскрываются адресами, переключатели скрываются.
- Всё содержание вынесено в `data/profile.json` — правка текста не касается кода.
